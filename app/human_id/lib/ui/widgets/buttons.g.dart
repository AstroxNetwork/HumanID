// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'buttons.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class BackButton extends StatelessWidget {
  const BackButton({
    Key? key,
    this.useCardColor = false,
  }) : super(key: key);

  final bool useCardColor;

  @override
  Widget build(BuildContext _context) => _backButton(
        _context,
        useCardColor: useCardColor,
      );
}

class SettingsButton extends StatelessWidget {
  const SettingsButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _settingsButton(_context);
}

class ScanButton extends StatelessWidget {
  const ScanButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _scanButton(_context);
}
