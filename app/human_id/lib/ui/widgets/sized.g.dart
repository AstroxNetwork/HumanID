// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'sized.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class Ver extends StatelessWidget {
  const Ver(
    this.v, {
    Key? key,
  }) : super(key: key);

  final double v;

  @override
  Widget build(BuildContext _context) => _ver(v);
}

class Hor extends StatelessWidget {
  const Hor(
    this.h, {
    Key? key,
  }) : super(key: key);

  final double h;

  @override
  Widget build(BuildContext _context) => _hor(h);
}

class SliverVer extends StatelessWidget {
  const SliverVer(
    this.v, {
    Key? key,
  }) : super(key: key);

  final double v;

  @override
  Widget build(BuildContext _context) => _sliverVer(v);
}

class SliverHor extends StatelessWidget {
  const SliverHor(
    this.h, {
    Key? key,
  }) : super(key: key);

  final double h;

  @override
  Widget build(BuildContext _context) => _sliverHor(h);
}
